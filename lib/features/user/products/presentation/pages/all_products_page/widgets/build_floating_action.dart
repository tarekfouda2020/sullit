part of'all_product_page_imports.dart';

class BuildFloatingAction extends StatelessWidget {
  const BuildFloatingAction({super.key});

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: Icon(Icons.filter_alt_outlined,color: context.colors.white,),
    );
  }
}
