part of 'restaurant_check_out_widgets_imports.dart';

class RestaurantTierFullNameSheet extends StatelessWidget {
  const RestaurantTierFullNameSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(color: context.colors.white),
    );
  }
}
