part of 'cart_widgets_imports.dart';

class DisableLayoutWidget extends StatelessWidget {
  final Widget child;
  const DisableLayoutWidget({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [],
    );
  }
}
